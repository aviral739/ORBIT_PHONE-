# Setup Instructions

1. Download the offline models (Qwen and Whisper).
2. Place the models in the `models/` directory (create if it doesn't exist).
3. Run the simulator tests:
   ```bash
   python3 simulator/EventSimulator.py
   ```
